# Dialyzer warnings to suppress. Intentionally empty — the tree is
# clean. Add `{"path/to/file.ex", "warning substring"}` entries here
# only for warnings that are genuinely wrong, with a comment saying why.
[]
